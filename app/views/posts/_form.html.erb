<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div class="error-explanation">
      <p><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</p>
      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, class: 'form-control' %>
  </div>
  <br>
  <%= submit_tag 'Save', class: 'btn btn-sm btn-success' %>
  <%= link_to root_path do %><div class="btn btn-sm btn-primary">Back</div><% end %>
<% end %>
